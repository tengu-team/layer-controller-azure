name: controller-azure
summary: Enables the use of azure-clouds in the Sojobo-api
description: |
  This subordenate charm copies the necessairy files to enable the use of azure-type clouds and restarts the api.
tags: ['misc']
subordinate: true
requires:
    sojobo:
        interface: sojobo
        scope: container
series: ['xenial']
